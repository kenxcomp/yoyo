{
  "$schema": "https://anthropic.com/claude-code/marketplace.schema.json",
  "name": "yoyo",
  "owner": {
    "name": "kenxcomp"
  },
  "metadata": {
    "description": "A curated collection of useful plugins for Claude Code",
    "version": "1.0.0"
  },
  "plugins": [
    {
      "name": "claude-md",
      "description": "A plugin for managing CLAUDE.md configurations with auto-update detection",
      "version": "1.1.0",
      "source": "./plugins/claude-md",
      "category": "productivity",
      "tags": ["configuration", "productivity", "hooks"]
    },
    {
      "name": "socratic-questioning",
      "description": "A plugin that guides Claude to use Socratic questioning methods with multiple choice questions to clarify unclear prompts",
      "version": "2.0.0",
      "source": "./plugins/socratic-questioning",
      "category": "productivity",
      "tags": ["thinking", "methodology", "skills", "questioning"]
    },
    {
      "name": "darwin",
      "description": "Automatic error-fixing plugin that offloads runtime error resolution to a dedicated agent, saving main conversation context",
      "version": "1.0.0",
      "source": "./plugins/darwin",
      "category": "productivity",
      "tags": ["error-fixing", "debugging", "agents", "automation"]
    },
    {
      "name": "plan-guardian",
      "description": "Plan review workflow plugin with a plan-reviewer agent for rigorous plan quality checks. Non-blocking by default — Claude autonomously decides when to review, or users trigger via /plan-review. v1.3.0 adds an opt-out codex review gate: a PreToolUse:ExitPlanMode hook intercepts plan-mode exits, instructs Claude to run /plan-guardian:codex-plan-review which loops `codex exec` against the plan and revises it until Codex emits NO_CONCERNS (set CODEX_PLAN_REVIEW=0 to disable). v1.4.x consolidated the loop's writes into a pre-approvable plan-review-helper.sh + a /plan-guardian:setup command of scoped allow-rules. v1.5.0 replaces the on-disk sha256 sentinel with an INLINE review marker carried inside the plan text (`<!-- codex-reviewed:<token> -->` on the last line; a CODEX_REVIEW_SECRET-keyed sha256 of the plan body, literal fallback when unset). The loop now writes nothing to disk during plan mode — the proof rides through tool_input.plan — sidestepping the upstream bug where the allow-list intermittently fails to suppress Write/Edit prompts under mode toggles; setup now needs only two Bash allow-rules (codex exec + helper).",
      "version": "1.5.0",
      "source": "./plugins/plan-guardian",
      "category": "productivity",
      "tags": ["planning", "review", "agents", "quality", "codex", "gate", "setup", "permissions", "marker"]
    },
    {
      "name": "bug-fix-testcase",
      "description": "End-to-end bug fix workflow with /bugfix command: analyze, research, plan, fix + parallel test generation, merge, and verify",
      "version": "1.1.1",
      "source": "./plugins/bug-fix-testcase",
      "category": "productivity",
      "tags": ["testing", "bugfix", "regression", "agents", "worktree"]
    },
    {
      "name": "codex-cr-loop",
      "description": "Codex review loop commands: iterate /codex:review until two consecutive clean rounds, with three completion modes (review-only stop, local merge, push + open PR). Always-on interaction-logic guard blocks user-visible UX changes (gestures, dialog/menu types, shortcuts, navigation) until the user explicitly confirms. Final report always ends with a Chinese business-impact summary module: a 3–8 line summary when commits change user-observable behavior, or a single literal line `无业务逻辑影响` for pure refactors. Preflight base-currency gate (v1.5.0) hard-stops the loop (rebase-first / abort) when the feature branch isn't built on top of its base; the base branch is auto-detected (main / master / any default).",
      "version": "1.5.0",
      "source": "./plugins/codex-cr-loop",
      "category": "productivity",
      "tags": ["codex", "review", "loop", "commands", "ci", "ux-guard"]
    },
    {
      "name": "form-base",
      "description": "HTML interaction base for Claude Code & Codex: agent-emitted forms (radio/checkbox/text/markdown + color/font/slider), rich-response views (business-prominent, technical-collapsed), and a switchable SessionStart-injected communication role (product-manager default; developer / lawyer / null switchable via /form-base:role). Role shapes answers only, never restricts tool use.",
      "version": "1.1.0",
      "source": "./plugins/form-base",
      "category": "productivity",
      "tags": ["mcp", "html", "forms", "responses", "ui", "codex", "agents", "role", "persona"]
    }
  ]
}
